tools = ["welder", "sensor", "motor"]

def check_tools(tools):
    for t in tools:
        print(f"{t}: OK")

check_tools(tools)

# Nota:
# Este script verifica el estado básico de herramientas industriales (ON/OFF).
# Es una demostración de lógica de control utilizada en procesos automatizados
# y en la integración de sensores, actuadores y equipos en entornos industriales.
